import styled from '@mui/system'
import React from 'react'

const cara = styled('div')({
    height: '50%,'
});

const Carousel = () => {
  return (
    <div>Carousel</div>
  )
}

export default Carousel